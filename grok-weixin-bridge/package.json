{
  "name": "grok-weixin-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bridge Weixin long-poll delivery to grok's OpenAI-compatible app-server.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/index.ts",
    "test": "node --test --import tsx test/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.8.3"
  }
}
